# Packages not listed here (numpy, pandas, scipy, matplotlib, seaborn, scikit-learn)
# are left to Colab's pre-installed versions. Pinning them causes binary
# incompatibilities when pip downgrades Colab's native numpy, breaking
# pre-compiled C extensions in pandas/scipy (numpy.dtype size error).
torch==2.2.0
transformers==4.38.2
datasets==2.18.0
wandb==0.16.4
gradio==4.21.0
tqdm==4.66.2
accelerate==0.27.2
ipywidgets==8.1.2
